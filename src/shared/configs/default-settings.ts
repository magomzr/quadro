// Configuración por defecto para nuevos tenants
export const DEFAULT_SETTINGS = {
  companyName: '', // Se completará con el nombre del tenant
  companyLogoUrl: undefined,
  currency: 'COP',
  locale: 'es-CO',
  timezone: 'UTC',
  invoicePrefix: 'INV-',
};
